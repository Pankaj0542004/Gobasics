package main

import "fmt"

func main() {
	// Constants in Go are fixed values that cannot be changed during the execution of a program.
	// They are defined using the 'const' keyword.

	const name string = "Pankaj Marathe"
	
	fmt.Println("Name:", name)
}